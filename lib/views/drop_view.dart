import 'package:flutter/material.dart';

class DropView extends StatelessWidget {
  const DropView({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('DropView');
  }
}
